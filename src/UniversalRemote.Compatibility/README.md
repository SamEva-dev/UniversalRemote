# UniversalRemote.Compatibility

Versioned compatibility evidence and operator integration decisions. This package deliberately contains no transport implementation and no secrets.

A profile is a product decision, not a promise that a device works. Physical device/firmware validation is required before a profile can be promoted to Stable.
